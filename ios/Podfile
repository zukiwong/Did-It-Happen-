platform :ios, '17.0'

project 'Runner', {
  'Debug'   => :debug,
  'Profile' => :release,
  'Release' => :release,
}

target 'Runner' do
  use_frameworks!
  # No external pods needed — pure SwiftUI + system frameworks
end
